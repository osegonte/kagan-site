import { aboutSchema } from './about'
import { servicesSchema } from './services'
import { videosSchema } from './videos'
import { heroSchema, contactSchema, socialSchema, settingsSchema } from './other'

export const schemas = [
  aboutSchema,
  servicesSchema,
  videosSchema,
  heroSchema,
  contactSchema,
  socialSchema,
  settingsSchema,
]